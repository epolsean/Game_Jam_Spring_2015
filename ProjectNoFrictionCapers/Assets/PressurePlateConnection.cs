using UnityEngine;
using System.Collections;

public class PressurePlateConnection : MonoBehaviour {

    public GameObject plate;
    public GameObject door;

	// Use this for initialization
	void Start () 
    {
        //LineRenderer lr = GetComponent<LineRenderer>();
        //lr.SetPosition(0, plate.transform.position);
        //lr.SetPosition(1, new Vector3(door.transform.position.x, door.transform.position.y, plate.transform.position.z));
        //lr.SetPosition(3, door.transform.position);
	}
	
	// Update is called once per frame
	void Update () 
    {
	
	}
}
